# Local dev: point at the FastAPI backend (default http://localhost:8000).
# Leave empty when using `npm run dev` with the vite proxy (set public/config.js apiBaseUrl to "").
VITE_API_BASE_URL=http://localhost:8000

# Optional: override vite dev proxy target
# VITE_API_PROXY_TARGET=http://localhost:8000
